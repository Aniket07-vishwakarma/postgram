version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install
      - npm install react-scripts --save-dev
      - npm install serverless --save-dev
  build:
    commands:
      - npm run build
